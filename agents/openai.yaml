interface:
  display_name: "Shixin 产品原型工作流"
  short_description: "把视觉参考收敛为可验证的桌面与网页产品原型"
  brand_color: "#2F7BFF"
  default_prompt: "使用 $shixin 把这个参考图或产品想法收敛成可运行、可验证、可发布的原型。"

policy:
  allow_implicit_invocation: true
